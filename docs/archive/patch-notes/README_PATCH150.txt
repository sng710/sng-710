PATCH 150 — uniform portrait face distance (display only)
=========================================================
- No image files are included or modified.
- 110 records with real portrait images receive display-only framing metadata.
- Face distance target: face bounding-box height ≈ 56% of the portrait circle.
- Uses only CSS/display properties: object-fit, object-position, scale, padding and background fill.
- No AI-generated/restored/extended image is used.
- 44 no-photo records keep the memorial candle fallback unchanged.
- No biography, gallery, video, link, name, place or other record data is changed.
- Cache version: 338.
