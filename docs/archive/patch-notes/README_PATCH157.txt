SNG-710 PATCH 157 — mobile settlement dropdown flow fix

Apply on top of the current site.

Replaces ONLY:
- index.html

Fix:
- On mobile, the open "לפי יישוב" dropdown now stays in normal page flow.
- It pushes the memorial grid downward instead of covering portrait cards and names.
- The settlement list remains scrollable with a bounded mobile height.

No people data, names, biographies, images, portrait settings, videos, JS, site.css,
person pages, or desktop layout are changed.
